name: Test React App
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get the repository code
        uses: actions/checkout@v3
      - name: Install dependdecies
        run: ci
